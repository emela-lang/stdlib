module ord

pub fn min<T: Ord>(a: T, b: T) -> T {
  if a < b {
    a
  } else {
    b
  }
}

pub fn max<T: Ord>(a: T, b: T) -> T {
  if a > b {
    a
  } else {
    b
  }
}

pub fn clamp<T: Ord>(value: T, low: T, high: T) -> T {
  if value < low {
    low
  } else if value > high {
    high
  } else {
    value
  }
}
